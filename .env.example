# Node.js 运行环境；本地开发使用 development。
NODE_ENV=development
# 本地服务监听端口。
PORT=5000

# Nacos 服务地址，格式为 host:port。
NACOS_SERVER=chat-nacos.lisfes.cn:8848
# Nacos 服务名称。
NACOS_SERVICE_NAME=chat-web-gateway-service
# Nacos 命名空间 ID（不是命名空间显示名称）。
NACOS_NAMESPACE=replace-with-nacos-namespace-id
# Nacos 配置中心 Data ID；配置分组默认使用 DEFAULT_GROUP。
NACOS_CONFIG_DATA_ID=chat-web-gateway-service.yaml
# Nacos 鉴权用户名和密码（仅保存于本地 .env，不提交仓库）。
NACOS_USERNAME=nacos
NACOS_PASSWORD=replace-with-nacos-password
# 是否注册服务；注册失败是否阻止启动使用默认值 false。
NACOS_REGISTER_ENABLED=true
